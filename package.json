{
  "name": "choicewright-first-key",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "for f in dist/*.js dist/arcade/*.js scripts/*.mjs; do node --check \"$f\" || exit 1; done",
    "test": "node tests/story.test.mjs"
  }
}
